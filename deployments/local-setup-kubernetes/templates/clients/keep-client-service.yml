apiVersion: v1
kind: Service
metadata:
  name: keep-client-{{ .clientIndex }}
  namespace: default
  labels:
    app: keep-client
    type: local
    id: '{{ .clientIndex }}'
spec:
  ports:
    - port: 3919
      targetPort: 3919
      name: tcp-3919
  selector:
    app: keep-client
    type: local
    id: '{{ .clientIndex }}'